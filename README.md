Created on StackBlitz using Vite and React 
The App works perfectly fine and utilises drag and drop features so todo's can be moved
up and down the list.
The main issue is the edit feature which seems to open but closes immediately when a key
is pressed. I haven't resolved the issue yet and can't understand why this is happening
as it worked fine before being split into components 
